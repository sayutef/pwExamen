import { ReplaceVowelsPipe } from './replace-vowels.pipe';

describe('ReplaceVowelsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceVowelsPipe();
    expect(pipe).toBeTruthy();
  });
});
